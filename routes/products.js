const express = require('express');
const routerProducts = express.Router();
// needs to be linked to something...
//const productDB = require('./../db/products.js');

module.exports = routerProducts;
